# ============================================
# Configuration partagée (tous les modes)
# Les URLs API sont dans les fichiers spécifiques :
#   .env.development  → npm run dev        (backend LOCAL)
#   .env.deployed     → npm run dev:deployed (backend RENDER)
#   .env.production   → npm run build       (build PROD)
# ============================================

# App Configuration
VITE_APP_NAME=Homi
VITE_APP_VERSION=1.0.0
